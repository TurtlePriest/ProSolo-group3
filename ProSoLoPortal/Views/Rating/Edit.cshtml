@model ProSoLoPortal.Models.Rating

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Rating</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RatingId" />
            <div class="form-group">
                <label asp-for="ProfileRefId" class="control-label"></label>
                <select asp-for="ProfileRefId" class="form-control" asp-items="ViewBag.ProfileRefId"></select>
                <span asp-validation-for="ProfileRefId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RatingNum" class="control-label"></label>
                <input asp-for="RatingNum" class="form-control" />
                <span asp-validation-for="RatingNum" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
